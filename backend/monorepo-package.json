{
  "name": "academic-recipe-hub-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for Academic Recipe Hub (Frontend & Backend)",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix frontend && dotnet restore backend/RecipeAPI",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend/RecipeAPI && dotnet run",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "dotnet build backend/RecipeAPI"
  }
}
